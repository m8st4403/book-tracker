# v4.13.16 回帰チェック追加
- 巻数表記の表示正規化、作品タイトルを勝手に統合しないこと、既存並べ替えへの非干渉、カードタイトルのフォントサイズ統一を実DOMで確認します。

# v4.11.3 回帰テスト追加

実際のDOMを使って、フィルターリセット・蔵書並び順・末尾数字のシリーズ巻抜け検出を開発時に自動検査します。

# v4.13.8 回帰チェック

# v4.13.11 UIブラウザ回帰テスト

蔵書の読書状態・お気に入り状態・蔵書一括操作UIの存在をチェック対象に追加しています。

# v4.10.1 追加チェック

バックアップのバージョン情報が `APP_VERSION` と一致することを確認する。

# Book Tracker — 開発ガード / 回帰チェック v4.10.0

## 目的

機能追加によって既存仕様を壊したり、過去に発生したバグを再発させたりする前に検出する。

## 3層構成

1. **仕様コメント**: `index.html` の `[BOOK TRACKER / SPEC GUARD]` に不変条件を記載。
2. **実行時ガード**: 開発版の設定から「仕様・回帰チェック」を実行できる。起動時にも静かに検査する。
3. **静的ガード**: `dev_guard.js` がHTMLを解析し、構文・重複・危険な直接操作・チェックボックス等を検査する。

## 実行時チェック

- 蔵書の purchaseStatus が全件 purchased
- purchaseStatus が許可された値のみ
- 検索結果の重複がない
- 全選択が checkbox
- 表示カードの蔵書バッジと books の状態が一致
- 正規登録関数が存在する

## 静的チェック

- JavaScript構文チェック
- 重複ID
- 重複した名前付きfunction
- `localStorage.clear()` の存在
- `getMeta(...).purchaseStatus =` の直接代入（正規化関数を除く）
- `.selectAll` / `.similarAll` に checkbox 指定があること
- `DEMO_ENABLED` と APP_VERSION の存在
- SPECコメントの存在
- 正規登録関数 `window.addBook` / `window.bulkAdd` の存在

## 変更時ルール

新機能を追加するときは、まず「どの既存不変条件に影響するか」を確認する。
状態を追加・変更する場合は状態遷移表を更新し、回帰チェックを1件以上追加する。
UIを変更する場合は「保存状態と表示状態が一致するか」を確認する。

## 変更履歴

- v4.10.0: 初版。仕様コメント、実行時仕様ガード、静的ガード、頻発バグ回帰ルールを導入。


## v4.13.8

v4.11.3をVisual Baselineとして扱い、機能追加による意図しないUI変更を防止する。シリーズ循環表示、ステータスタップ、タイトル一覧からの単巻展開、および5冊以上シリーズの後ろ4枚・5px刻み枠線表示を回帰確認する。


## v4.13.11 UIブラウザ回帰テスト

`node dev_guard.js index.html` は Chromium を DevTools Protocol 経由で起動し、390×844 の実機相当幅で小・中・大の3フォントを実際にレンダリングして検査する。文字の `scrollWidth/clientWidth`・`scrollHeight/clientHeight` を測定し、固定UIの文字はみ出しをFAILにする。蔵書カード寸法も中サイズを基準に比較し、フォント変更によるカード幅・高さの変化をFAILにする。カレンダーの「✓ 蔵書登録済み（タップで削除）」は非永続のテスト用予定を生成して実コード経路を検査する。


## v4.13.15 全量回帰テスト強化

- SPEC.md の明示仕様を棚卸しし、以下を実コード経路で回帰確認する：
  - ISBN-10/13のcanonical化と重複排除
  - 発売前登録禁止と蔵書=購入済み
  - 読了/未読・お気に入りの正規化・保存
  - 蔵書フィルターリセット
  - 個別表示／シリーズ表示の並び順
  - 5冊以上シリーズのデック寸法と5冊以下カード寸法の一致
  - シリーズ表示循環と開閉状態保持
  - 一括操作が現在の選択対象だけを変更すること
  - 登録／削除後の保存・再描画
  - バックアップ appVersion と APP_VERSION の一致
  - demo フラグによるサンプル分離
  - 背景画像の `cover` / center 表示とパネル約70%透明
- 静的ガードだけで済ませず、DOM寸法・computed style・localStorage・実関数結果を可能な範囲で検証する。


## v4.13.15 回帰強化
- 5冊以上タイトル行の実背景
- 背景画像のタブ間アジャスト固定
- 文字色自動補正
- 5冊以上シリーズのタイトル行はcomputed styleで背景が実際に存在すること、文字色が `--app-text` と一致することを確認する。
- 背景画像は固定背景レイヤーの `cover` / `center center` を確認し、ホーム・蔵書・検索・カレンダー・設定のタブ切替後も同一のcomputed styleを維持する。
- 文字色自動補正はON/OFFを実コードで切り替え、暗色／明色背景の両方で補正色のコントラストを検証する。
- CSSソースの存在だけでなく最終computed styleを検査し、後勝ちCSSによる偽陽性を防ぐ。

- 5冊以上重なりの上端非表示
- 5冊以上タイトル背景の濃さ比較
- pageshow後の初期タブ=ホーム

## v4.13.24 リリース前UI検証
UIのフォントサイズ修正では、CSS定義の存在確認だけを根拠にせず、Chromiumで実際に描画したDOMのcomputed styleを基準要素と比較する。対象は小・中・大の全設定で検査し、変更対象外の書籍タイトルも同時に監視する。ホーム「近日発売の注目書籍」も同じ実computed styleで検査する。詳細画面はalpha≈0.80を実DOMで検査する。修正後は配布ZIPを再展開し、展開後の`index.html`を最終検査する。


### v4.13.24 追加修正
- 蔵書の5冊以上表示で1冊を展開した際の「‹ タイトル一覧に戻る」は、ボタン枠・背景・ボタン風パディングを持たないテキスト操作表示とする。
- キーボード操作（Enter/Space）は維持する。
- 回帰テストでは実DOM要素がBUTTONではないこと、および背景・枠線・パディングがボタン風でないことを確認する。


## v4.13.31 表示回帰ガード
- ホーム／蔵書の蔵書総額コメントが同一の `priceSummaryText()` から生成されることを確認する。
- 縮小表示の5枚パネルにサブテキストが残っていないことを実DOMのcomputed styleで確認する。
- 390px相当幅でホームの価格コメントが `white-space:nowrap` / `text-overflow:ellipsis` / `overflow:hidden` によって切られていないことを確認する。
- 縮小版の蔵書総額数値に省略記号指定がなく、狭幅用フォント縮小ルールが存在することを確認する。
- 蔵書価格フィルターの選択肢・未登録／登録済み判定・リセットを回帰確認する。
- 配布ZIPを再展開し、展開後の `index.html` を最終検査対象とする。


## v4.13.31 外部リリースガード

`node dev_guard.js index.html` を配布前の必須ゲートとする。これは過去の不具合の再発テストだけでなく、6タブ全体を対象にした汎用UI不変条件、主要機能契約、代表的な状態遷移、文字切れ・overflow検出を行う。

アプリ内の開発用ガードは診断用。配布判定は外部ガードを正本とする。

配布ZIPは一度展開し、展開後の成果物に対して同じガードを実行する。

### v4.13.31 実行コマンド

```bash
npm test
```

`npm test` は通常の外部リリースガードに加え、意図的なUI overflowとAPI信頼度破壊を注入してテストがFAILすることまで確認する。したがって、単に「テストケースが存在する」だけでなく、代表的な検出能力もリリース前に検証する。

## v4.13.32 既存蔵書シリーズ再整理

- 蔵書タブの既存シリーズ再整理操作を静的／E2E契約で確認する。
- 正式シリーズ情報を安全に採用できる候補と、外伝等を拒否する候補をロジックテストする。
- 再整理処理がsingle-flightであることを確認する。
