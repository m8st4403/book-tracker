# 登録処理性能計測契約 v4.13.49

## 目的
実機で「登録中」が長い場合に、ユーザーがどの登録操作の結果なのかを明示したうえで、登録全体・API通信・API回数・データ処理・保存・画面更新を比較できるようにする。

## 操作ラベル
- サンプルデータ：1冊登録
- 検索結果：1冊登録
- 検索結果：選択した本を一括登録（対象IDを併記）
- 検索結果：表示中の全件を一括登録
- ISBN入力欄：チェックした本を一括登録
- 詳細画面：1冊登録
- カレンダー：1冊登録

## 原則
1. 操作ラベルなしの計測値をRelease計測として扱わない。
2. API通信時間と登録全体時間を同一視しない。
3. Provider別API時間とAPI呼び出し回数を可能な範囲で記録する。
4. 計測データはセッション内のみ保持し、蔵書データとは別扱いとする。
5. まず現状値を取得し、数値を確認してから高速化を実装する。
6. データ処理時間はAPI通信の重複時間を除外して計測する。
7. API通信が並行して発生する場合、API通信の表示値は各通信の実時間合計、データ処理はAPI通信区間の重複を除いた経過時間とする。
