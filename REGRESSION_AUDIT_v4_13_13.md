# Book Tracker 仕様・回帰テスト全量監査 v4.13.14

## 結論

v4.13.12までの回帰テストを、`SPEC.md` に明記された過去仕様・不変条件・BUG-001〜008・v4.13.11 UI基準・v4.13.12背景仕様と突合した。従来テストは「仕様カバレッジ全量」と称していたが、実際には次の条件が弱かった。

### 発見したテスト不足／偽陽性
1. **5冊以上シリーズのタイトル行背景**
   - 静的正規表現が、後から上書きする古いCSSルールにもマッチしていた。
   - そのため「PASS」でも最終表示が透明になる回帰を検出できなかった。
   - v4.13.14では最終DOMの`computedStyle.backgroundColor`を検査。
2. **背景画像のcover/center**
   - `body`のcomputed styleだけを見ており、実際の背景表示レイヤーを直接検査していなかった。
   - v4.13.14では固定`#appBackground`レイヤーを検査し、全6タブを切り替えて同一条件を確認。
3. **文字色自動補正**
   - 以前は関数の存在だけで、明暗背景に対して実際に4.5:1を満たすかを確認していなかった。
   - v4.13.14では暗色／明色背景、ON/OFFを実行検証。
4. **ISBNなし検索結果の重複排除**
   - ISBNありのcanonical重複だけを検査していた。
   - v4.13.14でタイトル・著者・出版社・発売日の複合キーを検査。
5. **ISBN-10/13の状態共有**
   - canonical関数の一致だけで、読了・お気に入り等のメタデータ共有までは検査していなかった。
   - 実装をcanonical key化し、回帰テストを追加。
6. **蔵書削除後の購入意向状態**
   - 所有解除と保存だけを検査していた。
   - v4.13.14では削除後に`purchaseStatus`が`purchased`のまま残らないことを検査し、実装も`wanted`へ戻す。
7. **v4.13.11固定UIケース**
   - 「開発用：仕様・回帰チェック」「蔵書登録済み（タップで削除）」は検査していたが、「詳細を見る」「ナチュラル」のうち前者は対象スコープから漏れていた。
   - 小・中・大すべてで確認対象へ追加。

## v4.13.14で固定したルール

- 5冊以上シリーズのタイトル行は背景を持ち、タイトル文字は`--app-text`。
- 背景画像は固定ビューポートレイヤーで`cover` / `center center`。タブ切替で基準が変わらない。
- 主要パネルは約70%透明（パネル側alpha約0.30）。
- 文字色自動補正は、背景オーバーレイ＋パネル透過後の実効背景色に対して4.5:1以上を目標とする。
- 5冊以上シリーズの一枚表示／全巻表示カードは5冊以下シリーズと同一寸法。
- 仕様を追加・変更した場合、対応する回帰テストも同時に追加する。

## 最終結果

`node dev_guard.js index.html` 実行結果：

- 静的ガード：全PASS
- 仕様カバレッジ：**29項目 PASS**
- UI文字はみ出し：小・中・大 PASS
- 蔵書カード内部はみ出し：小・中・大 PASS
- 蔵書カード寸法のフォント非依存：PASS（318×223）
- 固定UIケース：4項目すべて小・中・大 PASS


### v4.13.23 追加修正
- 蔵書の5冊以上表示で1冊を展開した際の「‹ タイトル一覧に戻る」は、ボタン枠・背景・ボタン風パディングを持たないテキスト操作表示とする。
- キーボード操作（Enter/Space）は維持する。
- 回帰テストでは実DOM要素がBUTTONではないこと、および背景・枠線・パディングがボタン風でないことを確認する。
