
## v4.13.130 調査結果コピー・クリア契約
- 蔵書タブの調査結果は、個別結果コピー・蔵書タブ結果まとめコピー・現在セッション全体コピーを提供する。
- 設定タブの調査結果は、個別結果コピー・設定タブ結果まとめコピー・現在セッション全体コピーを提供する。
- クリアは個別結果・タブ単位・現在セッション全体の3階層を提供する。
- クリア対象は調査結果と計測結果のみとし、蔵書、検索結果、設定値、カレンダー等の通常データを変更しない。
- 共有操作は結果を変更・消去しない。
# v4.13.130 現行リリース契約

- 現行リリース番号は `4.13.130` とする。
- `APP_VERSION`、`DEV_GUARD_VERSION`、`package.json`、README、設定画面のバージョン表示、APIキャッシュバスターは同一の現行リリース番号を参照する。
- 配布ZIPは再展開後の `index.html` を最終検査対象とし、バージョン不一致を含むいずれかのリリースゲートFAIL時は配布しない。
- 蔵書統計の縮小5列表示は、`#libraryStats` がビューポート上端へ到達した時点で `position: fixed` となり、`top: 0` を基準に画面最上部へ固定する。アプリヘッダーの文書上の高さを固定位置として使用しない。

# v4.13.31 追加仕様

- 巻数表記は `第N巻` / `N巻` / `N集` / `(N)` / `（N）` / 末尾の裸数字 `N` を認識し、表示上は「タイトル名 + 半角スペース + 数字」に統一する。
- 元のタイトルデータは変更せず、既存の並べ替えルールに使用するタイトル値も変更しない。
- 巻数の正規化は作品タイトルの意味を解釈しない。スピンオフ・外伝・短編集・番外編・関連作品・サブタイトル等は本編へ自動統合せず、タイトルが異なるものは別シリーズとして扱う。
- 「本を追加」「蔵書」「書籍検索」の各カード見出し、およびカレンダーの「yyyy-mm-dd の発売予定」「m月に発売された書籍」の見出しは、「類似作品を探す」のタイトルと同じフォントサイズ `19px × --font-scale` に統一する。書籍カード内の作品タイトルは従来サイズを維持する。
- ホームの「近日発売の注目書籍」見出しも「類似作品を探す」と同じフォントサイズに統一する。
- 詳細画面（`.detail-sheet`）の背景は透過なし（100%不透明）とし、この透過指定は詳細画面に限定する。

# v4.11.3 回帰修正

- フィルターリセットは検索文字列・全選択式フィルター・積読状態を含む蔵書絞り込み状態をすべて解除する。
- 並び順は個別表示とシリーズ表示の双方で選択値を反映する。
- 巻数は末尾の数字のみ（例：`ガードテスト 20`）でも認識する。
- 開発時にフィルターリセット、並び順、シリーズ巻抜けを自動回帰テストする。

# v4.13.8 蔵書UI・シリーズ表示改善

- 蔵書一覧は `books` を唯一の所有状態として扱う。
- 蔵書の購入状態は常に `purchased`。
- 読書状態は `read` / `unread` の2状態で管理する。
- お気に入りは boolean で管理する。
- 一括変更は正規の `updateBookMeta()` を通して保存・再描画する。
- 現在のフィルター結果だけを対象に一括操作する。
- 一括削除後は保存と再描画を行い、表示と状態を一致させる。

# v4.10.1 基盤仕上げ

- バックアップの `appVersion` は `APP_VERSION` を参照し、旧バージョンをハードコードしない。
- 登録・削除・購入状態変更は正規ルートを使用する。
- 登録後・削除後の検索結果表示を回帰対象とする。

# Book Tracker — 開発仕様・不変条件 v4.10.0

このファイルは「現在の仕様」と「過去に発生した再発防止ルール」をコード修正時のストッパーとして残すためのものです。

## API管理・自動フェイルオーバー仕様 v1.0

v4.13.24の次段階として、外部書籍APIはAdapter方式・項目別優先順位・Capability判定・Evidenceベースの情報信頼度チェック・自動フェイルオーバーを正式採用する。詳細は `API_MANAGEMENT_SPEC.md` を正本とする。

### 確定事項
- API全体ではなく情報項目単位でProvider優先順位を持つ。
- HTTP 200だけでは成功とせず、schema・必須フィールド・値・意味まで検証する。
- 各項目に信頼度を付与し、特にシリーズ統合と定価（税込）は低信頼値を自動確定しない。
- 新APIは未知のコードを自動実行せず、初回のみAdapterをアプリへ実装する。Adapter実装済みAPIは以後リモート設定で有効化・無効化・優先順位変更を可能にする。
- 蔵書総額は登録された本の確定済み `price.listPrice`（定価・税込）の合計とする。未確定価格と確定0円は加算しない。現在の販売価格では再計算しない。
- 定価未確定の本も蔵書登録でき、後から定価を確定・訂正できる。
- 実際の購入金額は定価とは別管理とし、シリーズ一括購入などは購入グループの `totalAmount` で総額のみ記録できる。購入総額は蔵書総額に影響しない。


## 1. 変更前に確認する仕様

### OWNERSHIP-001 / 蔵書の定義
`books` に存在する書籍は、ユーザーが所有する蔵書である。

### OWNERSHIP-002 / 蔵書の購入状態
蔵書の `purchaseStatus` は必ず `purchased`。
「欲しい」「予約済み」「購入しない」は未所有本にのみ許可する。

### OWNERSHIP-003 / 登録・削除
蔵書登録は所有状態を `owned` にする操作であり、登録された瞬間に購入状態は `purchased` とする。
蔵書削除は所有状態を解除する操作であり、購入意向の状態を再設定できる状態へ戻す。

### TIME-001 / 時系列
発売日、購入意向、所有状態は別概念。
「発売日になった」「発売済みになった」だけではユーザーの購入を意味しない。
発売前の本は原則として蔵書登録不可という現在の試作ルールを維持する。

### SEARCH-001 / 識別子
検索結果と蔵書の照合は `canonicalIsbn()` / `bookResultKey()` の共通キーを使う。
ISBN-10 と ISBN-13 は同一書籍として扱う。

### SEARCH-002 / 重複排除
同じ正規化ISBNの検索結果は1件だけ表示する。
ISBNがない場合はタイトル・著者・出版社・発売日の複合キーで重複を抑制する。

### STATE-001 / 状態変更の一連性
状態変更は「正規化 → 保存 → 関連UI再描画」までを1操作とする。
詳細画面だけ、一覧だけ、検索結果だけを更新する実装は禁止。

### STATE-002 / 状態の単一責務
購入状態の変更は `setPurchaseStatus()` または `updateBookMeta()` を経由する。
新機能から `getMeta(...).purchaseStatus = ...` を直接書き換えない。

### UI-001 / 表示整合性
保存データと画面表示が一致しなければならない。
特に「詳細では削除済みだが検索カードは登録済み」のような stale UI を許容しない。

### ARCH-001 / 正規ルート
登録処理は `window.addBook()` を正規ルートとする。
一括登録は同じルールを使い、独自の `books.push()` + 独自状態更新を増やさない。

### DEMO-001 / 開発用サンプル
サンプルデータは `demo:true` で通常データと区別する。
公開版では `DEMO_ENABLED=false` とし、ユーザーにサンプルを配布しない。
開発版ではバージョン更新時にサンプル削除状態をリセットできる。

## 2. 過去の頻発バグ（再発防止）

- BUG-001: データは更新されたが検索結果カードが古い → 状態変更後に関連ビューを再描画する。
- BUG-002: 同じISBNが複数表示 → canonical ISBNで検索結果を dedupe する。
- BUG-003: 全選択が入力欄になる → `.selectAll` / `.similarAll` は必ず `input[type=checkbox]`。
- BUG-004: 詳細画面と一覧画面で状態が違う → 表示側が独自状態を保持せず、共通の蔵書判定を使う。
- BUG-005: 機能追加で正規登録処理を迂回 → 登録・購入状態更新を正規関数へ集約する。
- BUG-006: 時系列の誤解 → 発売日と購入・所有を自動連結しない。
- BUG-007: サンプル削除後に通知だけ残る → `[hidden]` と display の競合を回避する。
- BUG-008: サンプル登録時に通常データへ昇格 → demoフラグを維持し、サンプル削除で関連データも除去する。

## 3. 仕様変更の扱い

上記ルールを変更する場合は「バグ修正」として黙って変更しない。
SPEC.md と DEV_GUARD.md の変更履歴に、変更理由・影響範囲・回帰チェック項目を記録する。

例：蔵書でも「欲しい」を許可したい場合は OWNERSHIP-002 の仕様変更であり、一覧・詳細・統計・バックアップ・推薦等への影響を確認する。


## v4.13.8 Visual Baseline / Feature-only

## v4.13.11 UI Browser Regression Foundation

蔵書タブのUI基準はv4.11.3。4.11.4〜4.11.7で明示された機能以外の見た目・色・サイズ・余白・配置は変更しない。タップ可能化だけを理由に青いボタン風へ変更しない。5冊以上シリーズは重ね表示→全巻表示→タイトル一覧→重ね表示の循環。タイトル一覧では読了/未読とお気に入りを縦に表示し、タイトルタップで該当巻だけを展開。蔵書/読了/お気に入りのステータスはタップ操作可能。5冊以上シリーズの重ね表示では、前面カードのデザインを変更せず、後ろ4枚を枠線のみで5px刻みに縦方向へずらす。前面カードを基準に後ろ4枚は右へ0.5px、1px、1.5px、2pxずつ配置する。全巻表示のカードサイズは5冊以下シリーズと統一する。


### v4.13.11 UI回帰テスト基盤
- v4.13.8の蔵書UIを視覚基準として扱い、フォントサイズ変更による蔵書カード寸法変化を禁止する。
- Chromiumの実レンダリングで390×844・小/中/大を検査し、ボタン等の文字はみ出しを数値判定する。
- 「開発用：仕様・回帰チェック」「蔵書登録済み（タップで削除）」「詳細を見る」「ナチュラル」を固定回帰ケースとする。


## v4.13.14 背景画像・回帰テスト強化

- 背景画像は画面全体に `cover` でアジャストし、中央基準で表示する。
- 背景画像設定時の主要パネルは約70%透明（パネル側の不透明度約30%）とし、背景画像が視認できること。
- 5冊以上シリーズの一枚表示（重ね表示の前面カード）は、5冊以下シリーズのカードと同一寸法とする。
- シリーズ表示でも蔵書の選択中の並び順をシリーズ単位の表示順へ反映する。
- 仕様に記載された状態遷移・登録/削除・ISBN正規化・バックアップ・サンプルデータ・シリーズ循環などは、可能な限り実DOM/実コード経路で回帰確認する。
- 仕様追加時は、仕様項目と回帰テスト項目を同時に更新し、「仕様だけ存在してテストがない」状態を残さない。


## v4.13.14 背景表示・文字色補正の回帰強化
- 背景画像は固定ビューポート背景レイヤーで `cover` / `center center` 表示し、タブ切替で背景の基準サイズが変化しない。
- 5冊以上シリーズのタイトル行は、従来の「少し濃い背景色」を維持しつつ約70%透明のパネル系背景とし、タイトル文字は `--app-text` を使用する。
- 文字色の自動補正は、背景画像の平均色そのものではなく、背景オーバーレイ＋パネル透過後の実効背景色を基準に4.5:1以上のコントラストを確保する。


## v4.13.14
- 5冊以上シリーズの重ね表示では、透過パネル越しに後ろ4枚の上端線が見えないこと。後ろ4枚は従来どおり枠線のみ、縦5px刻み・右0.5px刻みを維持する。
- 5冊以上シリーズのタイトル行背景は、5冊以下シリーズより少し濃い背景色を維持しつつ約70%透明とする。
- ページ再読み込み・bfcacheからの復帰を含め、初期表示タブは常にホームとする。


## v4.13.15 シリーズ巻数表記
- シリーズ判定は、末尾の「第N巻」「N巻」「N集」「(N)」「（N）」および既存仕様の末尾数字Nを同一の巻数Nとして扱う。
- 蔵書タブ・検索タブでの表示は「タイトル名 + 半角スペース + 数字のみ」に統一する。例：`作品名 2`。
- 保存されている元タイトルは変更せず、既存の並べ替えキー・並べ替えルールには干渉しない。
- 巻数を抽出できないタイトルは従来どおり表示する。

## v4.13.24 UI表示検証ルール
- 「類似作品を探す」を基準に、「本を追加」「蔵書」「書籍検索」およびカレンダーの指定見出しの実表示フォントサイズを一致させる。
- 書籍タイトル（`.title`）はカード見出しのサイズ変更対象外とし、文字サイズ設定ごとの既存サイズを維持する。
- フォントサイズ検証はCSS文字列の存在だけでPASSにせず、Chromium上の実DOMの`getComputedStyle()`で全サイズ（小・中・大）を比較する。
- 修正版のAPP_VERSION、README、SPEC、回帰テストのバージョンを一致させる。配布ZIPを再展開して、その中の`index.html`を最終検査対象とする。


### v4.13.24 追加修正
- 蔵書の5冊以上表示で1冊を展開した際の「‹ タイトル一覧に戻る」は、ボタン枠・背景・ボタン風パディングを持たないテキスト操作表示とする。
- キーボード操作（Enter/Space）は維持する。
- 回帰テストでは実DOM要素がBUTTONではないこと、および背景・枠線・パディングがボタン風でないことを確認する。


## v4.13.24 冊数別シリーズ表示設定
- 蔵書シリーズが **1冊** の場合は現状維持とし、従来の1冊表示を使用する。
- 蔵書シリーズが **2冊以上4冊以内** の場合は、シリーズ見出しをタップして「全巻表示」と「タイトルだけ表示」を切り替えられるようにする。
- 2〜4冊の初期状態は「全巻表示」。表示状態はシリーズ単位で端末内に保持する。
- 2〜4冊の「タイトルだけ表示」では、5冊以上シリーズと同じタイトル一覧UIを使用し、タイトルをタップすると該当巻だけを展開できる。「‹ タイトル一覧に戻る」は従来どおりボタン風ではないテキスト操作表示とする。
- **5冊以上** のシリーズは従来仕様を変更せず、「重ね表示 → 全巻表示 → タイトル一覧 → 重ね表示」の3状態循環を維持する。
- 2〜4冊の切替を追加しても、5冊以上の重ね表示・全巻表示カード寸法、1冊表示、既存の並び順には影響させない。


## 価格・購入金額（v4.13.28）
- **蔵書総額は登録された本の定価（税込）の合計**。
- 定価未確定でも蔵書登録可能。未確定は `price.listPrice=null` / `status="unconfirmed"` とし、蔵書総額に加算しない。
- 定価入力の空欄は未確定として登録する。
- 0円は明示確認後のみ `status="confirmed_zero"` として確定し、蔵書総額には加算しない。
- 1円以上は `status="confirmed"` として確定する。
- APIの定価は税込性と信頼度を検証し、条件を満たさない値は自動確定しない。
- Google Booksの実売価格（`retailPrice`）は定価の代替にしない。
- 一括登録でも価格未確定を理由に登録を中止しない。
- 実際の購入金額は定価と完全に分離する。複数冊のセット購入は `purchaseGroups.totalAmount` に総額だけを記録し、各巻へ購入単価を勝手に配分しない。
- 購入総額は蔵書総額に影響しない。後から各巻の定価が判明した場合のみ定価側を更新する。


## v4.13.29 蔵書サマリー・価格フィルター表示
- ホームと蔵書の「蔵書総額」コメントは `priceSummaryText()` の共通ルールで生成し、「定価確定：X/Y冊・未確定：Z冊」とする。
- 蔵書サマリーの縮小表示では説明文（サブテキスト）を非表示にし、5枚のラベルと数値だけを表示する。
- ホームの蔵書総額コメントは1行固定・省略表示を行わず、必要に応じて折り返して全文を表示する。
- 縮小表示の数値は重要情報なので省略記号で切らず、狭い画面では文字サイズを段階的に縮小する。
- 蔵書フィルターに「定価：すべて／未登録のみ／登録済み」を追加する。未登録は `status="unconfirmed"`、登録済みは確定価格または明示的に確定した0円を対象とする。
- フィルターリセットは価格フィルターも解除する。

## v4.13.31 テスト基盤仕様

- 過去の不具合だけを対象にした回帰テストではなく、仕様上の不変条件を母集団にする。
- ロジック、統合、実ブラウザE2E、全画面UI不変条件、視覚回帰、Mutation、配布パッケージ検査を分離する。
- 6タブ（ホーム／追加／蔵書／検索／カレンダー／設定）をすべてテスト対象とする。
- UIの文字切れ・overflow・viewport外脱落は特定の既知セレクタだけでなく、可視DOMを汎用走査する。
- 将来ロードマップ機能は `ROADMAP_TEST_MATRIX.md` に実装前からテスト契約を定義し、実装時にリリースゲートへ昇格する。
- テスト基盤自身についても代表的な意図的欠陥を注入し、FAILできることを確認する。
- 配布前にZIPを再展開し、展開後の成果物を最終検査する。


## v4.13.31 登録・検索競合制御

- 検索系操作はsingle-flightとし、同一操作の実行中の再タップを無視する。
- 個別登録・一括登録はISBNを共有する登録ロックを使用する。
- 一括登録では対象ISBNのロックを保存完了まで保持する。
- 検索／Resolverで確定した正式series.id、series.name、volumeNumberは登録データへ保持する。
- 価格が確定済みでもシリーズ情報が不足している場合のみResolverを補完呼び出しする。

## v4.13.32 既存蔵書のシリーズ再整理

- 蔵書タブの「既存蔵書のシリーズ再整理」でAPI Resolverを再利用し、既存本のシリーズ情報を再確認できる。
- 変更対象は `series` 情報のみ。タイトル、ISBN、作者、出版社、発売日、定価、読書状態、お気に入り、メモ等は変更しない。
- APIのシリーズ情報がHIGH以上で確定し、正規化した作品名とシリーズ名が一致する場合のみ自動更新する。
- 「外伝」「番外編」「スピンオフ」「短編集」「サイドストーリー」等を含む作品は自動統合しない。
- 作品名がシリーズ名と異なるタイトルは自動統合しない。巻数表記だけを正規化し、タイトル文言そのものは変更しない。
- 実行中はsingle-flightで二重実行を防止し、同一ISBNのResolver結果を同一実行内でキャッシュする。


### v4.13.35 データ操作ロックとシリーズ再整理
- サンプル蔵書は既存シリーズ再整理の対象外とする。
- 正式series.idが信頼できる場合、表示タイトルの字幕やv.3等の表記差だけを理由にシリーズ統合を拒否しない。
- 再整理・登録などのデータ操作は共通ロックで直列化し、処理中は検索を含む競合操作を開始させない。
- 長時間処理ボタンは処理中状態を即時表示し、完了後に元へ戻す。
- Resolver結果はセッション内でISBN単位にキャッシュし、不要な再通信を避ける。
- 一括登録の書誌準備は並行化するが、永続化は一度だけ行う。


### v4.13.36 全タブ排他制御・並び順タイブレーク
- データ変更操作は全タブで共通の排他制御対象とする。
- 登録、削除、蔵書メタデータ変更、価格・購入総額変更、シリーズ再整理、設定保存・データ復元等の競合開始を防止する。
- 検索実行中はデータ変更操作を開始できない。検索同士は既存の世代トークンで古い結果を無効化する。
- 並び順は選択項目を最優先キーとし、同値時は「作品名 → 巻数 → 登録順」を適用する。
- 作品名比較には巻数表記を除いた作品名を用い、巻数は数値として比較する。


### v4.13.38 ホーム／蔵書統計パネル共通化
- ホームと蔵書の「蔵書冊数／蔵書総額／購入予定／積読／お気に入り」は同一の `renderStatBoxes()` を使用する。
- 5枚のパネルは共通CSSで、2列グリッド・パネル寸法・余白・角丸・文字サイズ・色分けを統一する。
- 蔵書側のスクロール時コンパクト表示は既存仕様として維持し、通常表示ではホームと同じ内容・構造を使用する。


### v4.13.39 compact statistics label visibility
- 蔵書統計の縮小横一列表示では、5つの名称（蔵書冊数／蔵書総額／購入予定／積読／お気に入り）を必ず表示する。
- `.statbox > .muted` のような高いCSS優先度の包括セレクタで名称を隠さない。
- 実ブラウザの `getComputedStyle` と実寸（width/height）で5名称の表示状態を検証する。


## v4.13.40 検証体系
ルール台帳とルール→検証マトリクスを正本化。UIは存在だけでなく実寸・clip・overflow・viewportまで検証し、並び順は指定順→作品名→数値巻数→登録順を全sort modeで直接検証する。


## v4.13.40 データ操作カタログ
`OPERATION_CATALOG_v4_13_40.md` をデータ変更操作の正本とし、UI入口と関数入口の二重ロックで排他制御する。

## v4.13.44 ルール／検証体系監査

- 重要状態は、単にlocalStorageへ書き込めることではなく、実際のページreload後に意味が維持されることを検証する。
- 複数storageを変更する操作は、途中の保存失敗で部分更新を残さないよう、メモリ状態と保存状態を元へ戻す。
- バックアップはschemaVersion、appVersion、許可されたstorage keyの範囲を検証対象とする。
- package.json、APP_VERSION、DEV_GUARD_VERSIONは同一バージョンで配布する。
- 新機能の実装前に、既存ルールに「検証方法」「失敗時の期待状態」「reload後の確認」が存在するか監査する。
