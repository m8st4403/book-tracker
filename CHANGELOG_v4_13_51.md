# v4.13.51

## 修正
- 単冊登録のユーザー向け完了/失敗通知を、登録計測の `finish()` 後に表示する構造へ変更。
- これにより、登録全体の計測時間に `alert()` の表示待ち時間を含めない。
- カレンダー・検索カード・詳細画面の単冊登録入口で通知を遅延表示。
- v4.13.50までの「API通信をデータ処理から除外」「1操作=1計測結果」を維持。
- 同じ構造を静的ガードで監視。

## 実機確認待ち
- カレンダーから1冊登録した際、登録全体と各内訳の差が不自然な固定待ち時間を含まないこと。
- 計測結果が1件であること。
- 登録完了通知が表示されること。
