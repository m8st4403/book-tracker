# v4.13.48 修正内容

## 登録処理の計測：二重記録修正

- `window.addBook()` に渡された計測トークンの終了処理を、単冊登録の操作入口側に統一。
- カレンダー／検索結果／詳細画面の「1冊登録」で、1回の操作につき計測結果が1件になる構造へ修正。
- `addBook()` は登録結果を返す責務を維持し、caller-owned metrics の `finish()` を呼ばない。
- `dev_guard.js` に、`addBook()` が caller-owned metrics を終了しないことを検査する静的回帰チェックを追加。
- アプリ／package／guard のバージョンを 4.13.48 に更新。

## 今回は未変更

- Google Books 約12秒通信の原因調査
- ISBN `9784088720715` の追加タブ検索失敗の調査
- API通信／データ処理の計測区間の再設計

これらは次の優先作業として独立して調査する。
