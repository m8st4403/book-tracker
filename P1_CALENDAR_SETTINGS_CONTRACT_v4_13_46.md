# P1 カレンダー／設定／ICS／発売通知 検証契約 v4.13.46

## 対象

- カレンダー設定（週始まり、蔵書／関連／おすすめ表示、今日を開く、ICS表示）
- カレンダータブ内の表示フィルター（一時状態）
- 関連図書／おすすめのカレンダー追加
- ICS生成
- 発売通知の対象期間
- 設定全項目の保存失敗時ロールバック

## 不変条件

1. 設定値は保存→再読込で意味を維持する。
2. 設定保存に失敗した場合、メモリ上の設定と設定UIも最後に保存された状態へ戻る。
3. カレンダータブ内の表示フィルターは一時状態で、タブを開き直すと保存済み設定を初期値として再適用する。
4. カレンダー追加の保存に失敗した場合、追加前の `calendarExtras` に戻る。
5. 発売日は時刻を持たないため、ICSでは `DTSTART;VALUE=DATE` を使う。
6. ICSのSUMMARY/DESCRIPTIONはRFC 5545の値エスケープ（\、;、,、改行）を行う。
7. 同じイベントは同じUIDを生成し、イベント識別情報を変えればUIDも変わる。
8. 発売通知対象は「今日～7日後」を含み、通知OFF・不正日付は除外する。

## 検証フロー

仕様不変条件 → 入力境界 → 状態遷移 → 保存原子性 → reload → 関連画面整合 → backup/restore → UI実寸 → Mutation → ZIP再展開。

## v4.13.46 Gate

- `E2E-SETTINGS-001` 全設定項目のsave/read round-trip
- `E2E-SETTINGS-002` 設定保存失敗時のメモリ／UIロールバック
- `E2E-CALENDAR-001` 一時フィルターの保存済み初期値への再同期
- `E2E-CALENDAR-002` カレンダー追加失敗時のロールバック
- `E2E-ICS-001` 日付形式・エスケープ・安定UID
- `E2E-NOTIFY-001` 今日～7日後の通知対象境界
