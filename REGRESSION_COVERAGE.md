# Book Tracker — 仕様／回帰テスト対応表 v4.13.26

「仕様に明記されているのに回帰テストがない」状態を防ぐための対応表です。

## 仕様項目 → 回帰確認

| 仕様 | 回帰確認 | 方法 |
|---|---|---|
| フィルターリセット | フィルター全解除 | 実DOM |
| 個別表示の並び順 | 蔵書並び順 | 実DOM（既存） |
| シリーズ表示の並び順 | シリーズ表示の並び順 | 実DOM |
| 末尾数字の巻数／巻抜け | シリーズ巻抜け検出 | 実コード（既存） |
| books＝唯一の所有状態 | 登録=蔵書=購入済み、表示と状態 | 実コード／DOM |
| 蔵書 purchaseStatus=purchased | 登録=蔵書=購入済み | 実コード |
| read/unread の2状態 | 読了/お気に入り正規化、不正メタデータの正規化 | 実コード |
| favorite は boolean | 読了/お気に入り正規化、不正メタデータの正規化 | 実コード |
| 一括変更は updateBookMeta 経由 | 一括変更はupdateBookMeta経由 | 静的＋実DOM |
| 現在のフィルター結果だけ一括操作 | 一括操作は現在のフィルター対象のみ | 実DOM |
| 一括削除後に保存・再描画 | 一括削除は保存後再描画 | 静的 |
| バックアップ appVersion | バックアップappVersion | 実コード |
| 登録・削除・購入状態の正規ルート | 正規登録関数、登録/削除フロー、静的経路確認 | 実コード＋静的 |
| 登録後の検索結果表示 | 登録/削除後の検索結果表示 | 実DOM |
| 削除後の検索結果表示 | 登録/削除後の検索結果表示 | 実DOM |
| 登録＝所有、削除＝所有解除 | 登録=蔵書=購入済み、蔵書削除=所有解除+保存＋購入意向リセット | 実コード |
| 発売日と所有は別概念 | 発売日=購入ではない | 実コード |
| 発売前登録不可 | 発売前登録禁止 | 実コード |
| ISBN-10/13同一扱い | ISBN-10/13 canonical一致 | 実コード |
| 同一ISBN重複排除 | 検索結果ISBN重複排除＋既存結果重複チェック | 実コード |
| ISBNなし複合キー重複排除 | タイトル・著者・出版社・発売日の複合キーで重複抑制 | 実コード |
| ISBN-10/13状態共有 | 同一書籍のメタデータをcanonical ISBNで共有 | 実コード |
| 状態変更後に関連UI再描画 | 状態変更後の関連UI再描画 | 実DOM |
| 画面ごとに独自状態を持たない | 表示と蔵書状態、正規化経路 | DOM＋静的 |
| demo:true でサンプル分離 | サンプルdemo分離、サンプル削除の連動 | 実コード |
| BUG-001 stale UI | 状態変更後の関連UI再描画 | 実DOM |
| BUG-002 ISBN重複 | 検索結果ISBN重複排除 | 実コード |
| BUG-003 selectAll checkbox | 全選択は checkbox | 静的＋実行時 |
| BUG-004 詳細／一覧状態不一致 | 表示と蔵書状態 | 実DOM |
| BUG-005 正規登録迂回 | 正規関数＋静的経路確認 | 静的 |
| BUG-006 発売日＝購入の誤認 | 発売日=購入ではない、発売前登録禁止 | 実コード |
| BUG-007 サンプル削除後の通知等 | サンプル削除の連動 | 実コード |
| BUG-008 サンプル通常データ昇格 | サンプルdemo分離 | 実コード |
| シリーズ循環 deck→list→title→deck | シリーズ表示循環 | 実コード |
| シリーズ開閉状態保持 | シリーズ開閉状態保持 | localStorage |
| タイトル一覧から単巻展開 | タイトル一覧から単巻展開 | 実DOM |
| 5冊以上後ろ4枚・5px刻み・右0.5px刻み | 後ろ4枚5px刻み枠線 | 静的CSS |
| 5冊以上タイトル行の背景／文字色 | 5冊以上タイトル行の実背景＋文字色 | 実DOM computed style（最終ルール） |
| 5冊以上タイトル行の後勝ちCSS耐性 | 後段のCSSで背景が消えていないこと | 実DOM computed style |
| 5冊以上一枚表示カード＝5冊以下カード | 5冊以上一枚表示カード寸法 | 実DOM寸法 |
| 5冊以上全巻表示カード＝5冊以下カード | 5冊以上全巻表示カード寸法 | 実DOM寸法 |
| フォント変更でカード寸法を変えない | 蔵書カード寸法のフォント非依存 | 390×844実DOM |
| 固定UIの文字はみ出し防止 | UI文字はみ出し、必須UIケース | 390×844実DOM |
| 背景画像を画面にcover表示 | 固定背景レイヤーのcover/center＋全タブ切替 | 実DOM computed style |
| パネル約70%透明 | 主要パネルのcomputed alpha=0.3前後 | 実DOM computed style |
| 文字色自動補正 | 明暗背景で4.5:1以上＋OFF時は原色維持 | 実コード＋実DOM |

## 実行方法

```bash
node dev_guard.js index.html
```

Chromiumを使ったUI回帰では390×844で小・中・大フォントをレンダリングします。
ストレージは、開発ガードの `setDocumentContent` 実行環境でもテストが止まらないよう、利用できない場合のみテスト専用のlocalStorage互換層を使用します。

## v4.13.14で追加した重要な回帰

1. 5冊以上シリーズの一枚表示カード寸法
2. 5冊以上シリーズの全巻表示カード寸法
3. シリーズ表示の並び順
4. 状態変更後の関連UI再描画
5. 登録／削除後の検索結果表示
6. 一括操作のフィルター対象限定
7. サンプル削除の連動
8. 背景画像のcover/center
9. パネル透明度約70%
10. ISBN-10/13 canonical化

これにより、仕様だけ存在して実装変更時に検査されない主要な回帰ポイントを解消しています。

## v4.13.14 追加回帰
- 5冊以上タイトル行の実背景（最終computed style）
- 背景画像のタブ間アジャスト固定
- 文字色自動補正（明暗背景＋OFF）
- ISBNなし複合キー重複排除
- ISBN-10/13状態共有
- 蔵書削除時の購入意向リセット
- 固定UI「詳細を見る」の小・中・大表示
- 5冊以上タイトル行の背景が後勝ちCSSで消えていないこと
- 背景固定レイヤーがタブ切替で変化しないこと
- 自動文字色補正が実効パネル背景に対して機能すること

| 5冊以上重なりの上端非表示 | 透過パネル越しに後ろカードの上端線が見えない | 実DOM computed style |
| 5冊以上タイトル背景の濃さ | 5冊以下より少し濃い | 実DOM RGB比較 |
| 初期表示タブ=ホーム | reload/pageshow後にホーム | 実DOM pageshow |


## v4.13.15 追加回帰
| 仕様 | 回帰確認 | 方法 |
|---|---|---|
| 「1巻」「第1巻」「(1)」「（1）」「1」を同じ巻数として扱う | 5形式のparse/volume/seriesKey一致 | 実コード |
| 蔵書・検索の巻数表示をタイトル＋数字に統一 | `card()` の表示結果を確認 | 実コード |
| 元タイトルを変更せず並べ替えルールに干渉しない | stored titleをsort keyとして比較 | 実コード |
| シリーズ混在タイトルを同一グループにする | 5形式を同一seriesKeyへ集約 | 実コード |

| 巻数表記の正規化 | 1巻/第2巻/(3)/（4）/5を表示統一 | 実DOM＋実コード |
| スピンオフ等の別シリーズ扱い | 本編と外伝を自動統合しない | 実DOM |
| 既存並べ替えへの非干渉 | タイトルソートは元タイトル、巻数ソートは数値 | 実コード |
| カードタイトル文字サイズ | 類似作品を探す・本を追加・蔵書・書籍検索・カレンダー・近日発売の注目書籍で一致 | 実DOM |
| 詳細画面透過 | 詳細画面の背景のみ透過なし（100%不透明） | 実DOM |

## v4.13.24 追加回帰
- カード見出しの実computed styleを「類似作品を探す」基準で比較（小・中・大）。
- ホーム「近日発売の注目書籍」の見出しも同じ基準で実computed styleを比較。
- 詳細画面の背景computed styleを確認し、透過なし（100%不透明）を検証。
- 「本を追加」「蔵書」「書籍検索」の3見出しを明示的な文言で特定し、対象欠落をPASSにしない。
- 書籍タイトルは各文字サイズ設定で既存の`16px × font-scale`を維持することを確認。
- APP_VERSIONと配布物のバージョン整合を検査。


### v4.13.24 追加修正
- 蔵書の5冊以上表示で1冊を展開した際の「‹ タイトル一覧に戻る」は、ボタン枠・背景・ボタン風パディングを持たないテキスト操作表示とする。
- キーボード操作（Enter/Space）は維持する。
- 回帰テストでは実DOM要素がBUTTONではないこと、および背景・枠線・パディングがボタン風でないことを確認する。


## v4.13.24 冊数別シリーズ表示回帰
| 条件 | 期待動作 | 検証 |
|---|---|---|
| 1冊 | 現状維持の1冊表示 | 実DOM |
| 2冊 | 全巻表示 ↔ タイトルだけ表示 | 実DOM＋状態遷移 |
| 3冊 | 全巻表示 ↔ タイトルだけ表示 | 実コード |
| 4冊 | 全巻表示 ↔ タイトルだけ表示 | 実コード |
| 5冊以上 | 従来の3状態循環を維持 | 実DOM＋状態遷移 |
| 2〜4冊のカード寸法 | 従来の単巻カード寸法を維持 | 実DOM |


## API管理仕様 v1.0（実装前の回帰項目）

| ID | 項目 | 確認内容 | 状態 |
|---|---|---|---|
| API-001 | Adapter分離 | Provider固有レスポンスを共通BookRecordへ変換 | 仕様確定・未実装 |
| API-002 | 項目別優先順位 | API全体ではなくfield単位で候補を選択 | 仕様確定・未実装 |
| API-003 | Capability | 必要Capabilityを持たないProviderを候補から除外 | 仕様確定・未実装 |
| API-004 | Schema検証 | HTTP 200でもschema不一致なら失敗扱い | 仕様確定・未実装 |
| API-005 | 必須Field検証 | 必須値欠損なら次候補へフェイルオーバー | 仕様確定・未実装 |
| API-006 | 値・意味検証 | ISBN、日付、価格、税区分等を検証 | 仕様確定・未実装 |
| API-007 | 信頼度 | EvidenceからVERIFIED/HIGH/MEDIUM/LOW/UNKNOWNを判定 | 仕様確定・未実装 |
| API-008 | Critical閾値 | series.id/name/volume/listPrice/taxIncludedは低信頼値を自動確定しない | 仕様確定・未実装 |
| API-009 | 複数Provider矛盾 | Critical項目の不一致を単純多数決で確定しない | 仕様確定・未実装 |
| API-010 | 自動Failover | timeout/HTTP/schema/field/confidence失敗で次候補へ移行 | 仕様確定・未実装 |
| API-011 | 新API | 未知APIのコード自動取得・実行を行わない | 仕様確定・未実装 |
| API-012 | Remote Config | Adapter実装済みProviderを後から有効化/無効化/順位変更 | 仕様確定・未実装 |
| API-013 | 価格固定 | 登録時の定価（税込）を保存し、後日のAPI価格変更で蔵書総額を変更しない | 仕様確定・未実装 |
| API-014 | Cache | API停止時に既存キャッシュを利用できるが古いCritical値を新規確定しない | 仕様確定・未実装 |
| API-015 | Contract Test | Providerごとに到達性・schema・主要field・意味を検証 | 仕様確定・未実装 |

> v4.13.26ではGoogle Books/openBDの主要呼出しをAdapter経由へ移行した。楽天Books/NDLの実Adapter、複数Providerの実フェイルオーバー、定価（税込）の正式保存統合は後続段階で実装する。

## API Management v1.0 — Phase 2
- API-001〜005: Phase 1のProvider / Capability / field-priority / Evidence / Critical / Remote Config基盤を維持。
- API-006: Google Books / openBD Adapterの入口が存在する。
- API-007: 既存のISBN照会・Google Books検索がAPI管理Adapter経由になり、直接Provider URLを呼び出さない。
- API-008: index.html側からGoogle Books/openBDのProvider直接呼出しが残っていない。
- API-009: Adapterが正規化済みの共通書籍データを返す。

> Phase 2では既存Google Books/openBDの呼出し経路をAdapterへ移行した。楽天Books/NDLの実Adapter追加、複数Providerの実フェイルオーバー、定価（税込）の正式保存統合は後続段階で実装する。
